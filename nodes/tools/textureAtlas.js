export class TextureAtlas {
    addImage(url, id) {

    }
    
    build() {

    }

    /**
     * Canvas with textures drawn into it,
     * only available after you run build().
     * @returns {HTMLCanvasElement} 
     */
    getTexture() {

    }

    /**
     * Map from texture id to a rectangle within the canvas.
     * Only available after you run build().
     * @returns {Map<string, {}>}
     */
    getMap() {

    }
}